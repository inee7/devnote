# 1.3 로직과 떨어진 데이터 클래스 
#### 1.3.1 사양이 변경 될때 문제 
- 클래스 밖에서도 자유롭게 변경할 수 있는 구조에 데이터를 갖고 있기만 하고 로직은 다른 클래스에 있는 클래스를 데이터 클래스라고 부름
~~~java
public class contractAmount {
	public int amountIncludingTax;
	public BigDecimal salesTaxRate;
}

public class ContractManager { 
	public ContractAmount contractAmount; 
	
	public void conclude() {
		//생략
		contractAmount = new ContractAmount();
		contractAmount.amountIncludingTax = 1000;
		//생략
	}
}
~~~
![](1.3%20%EB%A1%9C%EC%A7%81%EA%B3%BC%20%EB%96%A8%EC%96%B4%EC%A7%84%20%EB%8D%B0%EC%9D%B4%ED%84%B0%20%ED%81%B4%EB%9E%98%EC%8A%A4/sketch.png)
- 이처럼 데이터클래스 하나를 여러 Manager에서 로직을 들고 있으면 버그 날 확률이 높아진다 
- 데이터와 로직 등이 분산되어 있는것을 응집도가 낮은 구조라고 함 
#### 1.3.2 코드 중복 
- 관련된 코드가 서로 멀리 떨어져 있으면 데이터클래스만 보고 아직 구현 되지 않은 로직이라 생각하고 중복 코드를 만들게 된다 
#### 1.3.4 수정 누락 
- 코드 중복이 많으면 사양 변경될 때 중복된 코드를 모두 고쳐야 한다. 하지만 놓칠수 있기에 결국 버그 유발 
#### 1.3.4 가독성 저하 
- 분산 된 코드는 중복된 코드를 포함해서 관련된 정보를 찾는데 시간이 오래 걸림 
#### 1.3.5 초기화 되지 않는 상태 
- `ContractAmount amount = new ContractAmount();  amount.salesTaxRate.toString(); `
- npe 발생 할수 있다 
#### 1.3.6 잘못된 값 할당
- 초기화할때 유효성 검사를 하지 않아 추후 로직에서 검사 로직이 필요 하게 된다 

#dev/book/내코드가그렇게이상한가요