# 무인자생성자 없어도 역직렬화 
자바에서 역직렬화할때 jackson라이브러리는 리플렉션을 쓴다 
리플렉션은 생성자의 인자를 확인할 수 없다
그래서 무인자 생성자를 가지고 객체를 생성해 필드에 집어 넣는다
그래서 무인자 생성자가 있어야하는데 코틀린의 경우 무인자가 아니라도 가능하게 해주는게 있다 
`jackson-module-kotlin`

그리고 리플렉션은 접근제어를 무시하기에 생성자가 private도 가능하다 

#kotlin